imports:
    - { resource: config.yml }

framework:
    test: ~
    session:
        storage_id: session.storage.filesystem

#
# KOTTI SECURITY
#
truelab_kotti_security:
    auth:
        secret: qwerty
